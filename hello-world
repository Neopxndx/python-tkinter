from tkinter import *

class Alumn:
    def __init__(self,window):
        self.window = window
        self.window.title('Hello World')

if __name__=="__main__":
    window = Tk()
    aplication = Alumn(window)
    window.mainloop()
